export class LoginResponse {
        id: number;
        email: string;
        employeeId: number;
        errorMessage: string;
        successMessage: string;
        token: string;
        refreshToken: string 
}