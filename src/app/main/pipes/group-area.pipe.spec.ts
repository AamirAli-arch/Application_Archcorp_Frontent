import { GroupAreaPipe } from './group-area.pipe';

describe('GroupAreaPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupAreaPipe();
    expect(pipe).toBeTruthy();
  });
});
