import { UniqueResourcePipe } from './unique-resource.pipe';

describe('UniqueResourcePipe', () => {
  it('create an instance', () => {
    const pipe = new UniqueResourcePipe();
    expect(pipe).toBeTruthy();
  });
});
