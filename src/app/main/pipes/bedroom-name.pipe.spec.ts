import { BedroomNamePipe } from './bedroom-name.pipe';

describe('BedroomNamePipe', () => {
  it('create an instance', () => {
    const pipe = new BedroomNamePipe();
    expect(pipe).toBeTruthy();
  });
});
